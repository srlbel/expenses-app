{
	"name": "expenses-app",
	"private": true,
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"dev": "bun run --parallel --filter '*' dev",
		"lint": "oxlint .",
		"lint:quiet": "oxlint --quiet .",
		"lint:fix": "oxlint --fix .",
		"fmt": "oxfmt .",
		"fmt:check": "oxfmt --check ."
	},
	"devDependencies": {
		"oxfmt": "^0.65.0",
		"oxlint": "^1.80.0"
	}
}
